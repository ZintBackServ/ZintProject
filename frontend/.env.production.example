# Set BEFORE running: npm run build
#
# Option A — API proxied on same domain (Apache .htaccess forwards /user, /course, etc.)
VITE_API_URL=https://zintinstitute.in
#
# Option B — API on separate subdomain
# VITE_API_URL=https://api.zintinstitute.in
